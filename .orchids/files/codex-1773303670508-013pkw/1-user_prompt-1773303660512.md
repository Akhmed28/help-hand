Replace the main landing page and connect it to the existing backend.
The project is an AI-powered coding platform (Vibe Coding) — similar to Replit, Lovable, and Cursor. The goal is to swap out the old homepage with the new cloned HelpHand page, and wire it up to the existing backend logic.

Step 1 — Remove the old main page:

Delete the current index.html (or equivalent root landing page)
Do not touch any other pages, routes, or files
Keep all existing backend files, API routes, and server logic completely intact

Step 2 — Set the new page as the main page:

Take the cloned HelpHand page (already translated to Russian, already branded as HelpHand)
Set it as the new index.html / root route /
Make sure it loads correctly as the homepage

Step 3 — Connect the frontend to the existing backend:

Find the main input field on the new page (where the user types a prompt to generate a project)
Connect this input to the existing backend endpoint that handles project/code generation
The form submission or button click should trigger the same API call / function that was used on the old homepage
Preserve the exact same request format, parameters, and response handling as before
If the old page used WebSockets, streaming, or polling — replicate that same connection method on the new page

Step 4 — Ensure core functionality works:

User types a prompt → hits the generate/create button → backend processes it → result is shown
This full flow must work end-to-end on the new homepage
Do not break any existing sessions, auth flows, or routing logic

Step 5 — Verify:

The new HelpHand page loads at the root URL
All styles, animations, and UI elements display correctly
The AI code generation feature works exactly as it did before
No console errors related to missing endpoints or broken API calls

Do not change:

Backend logic, API routes, or server configuration
Any other pages or features of the platform
Database connections or environment variables